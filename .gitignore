# ⚠️ NEVER commit customer files to this repo
# JSON uploads — customer pricing data, must never be pushed
upload json file here/*.json
upload json file here/*.html

# Also block old folder name just in case
uploads/*.json
uploads/*.html

# OS
.DS_Store
Thumbs.db
